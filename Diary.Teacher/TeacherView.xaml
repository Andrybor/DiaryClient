<UserControl x:Class="Diary.Teacher.TeacherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Diary.Teacher"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <mah:MetroAnimatedTabControl x:Name="Items"
                                 DisplayMemberPath="DisplayName"
                                 SelectedItem="{Binding ActiveItem}">
        <mah:MetroAnimatedTabControl.ItemContainerStyle>
            <Style TargetType="{x:Type mah:MetroTabItem}">
                <Setter Property="HeaderTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock FontSize="25" Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </mah:MetroAnimatedTabControl.ItemContainerStyle>
    </mah:MetroAnimatedTabControl>
</UserControl>
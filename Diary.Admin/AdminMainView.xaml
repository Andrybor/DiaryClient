<Grid x:Class="Diary.Admin.AdminMainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls">
    <Controls:MetroAnimatedTabControl x:Name="Items"
                                      DisplayMemberPath="DisplayName"
                                      SelectedItem="{Binding ActiveItem}">
        <Controls:MetroAnimatedTabControl.ItemContainerStyle>
            <Style TargetType="{x:Type Controls:MetroTabItem}">
                <Setter Property="HeaderTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock FontSize="25" Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Controls:MetroAnimatedTabControl.ItemContainerStyle>
    </Controls:MetroAnimatedTabControl>
</Grid>